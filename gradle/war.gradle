apply plugin: "war"

bootWar {
    mainClassName = "com.singsaver.creditcard.CreditcardApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
